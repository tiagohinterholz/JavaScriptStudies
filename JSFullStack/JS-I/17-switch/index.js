const resultado = prompt("Escolha uma alternativa: a), b), c)")

switch (resultado) {
    case 'a':
        alert(`O resultado é ${resultado}`)
        break
    case 'b':
        alert(`O resultado é ${resultado}`)
        break
    case 'c':
        alert(`O resultado é ${resultado}`)
        break
    default:
        alert("Finalizando")
}